<HeadLayout />
{{page-title "SkyLines"}}

<ProgressBar/>
<NotificationContainer @zindex="7000"/>

{{#if onFreestyleRoute}}
  {{outlet}}
{{else}}
  <NavBar @onSearch={{this.search}} />
  {{outlet}}
{{/if}}