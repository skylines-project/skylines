{{did-insert this.setVisible @visible}}
{{did-update this.setVisible @visible}}
{{#each contests as |contest|}}
  {{#if contest}}
    <ContestLayerFeature @source={{source}} @contest={{contest}} />
  {{/if}}
{{/each}}