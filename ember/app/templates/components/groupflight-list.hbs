<thead>
<tr>
  {{#if showDate}}
    <SortableTableHeader @column="date" @sortColumn={{sortColumn}} @sortOrder={{sortOrder}} @defaultOrder="desc" @class="date">{{t "date-compiled"}}</SortableTableHeader>
  {{/if}}

  {{#if showClub}}
    <SortableTableHeader @column="club" @sortColumn={{sortColumn}} @sortOrder={{sortOrder}} @class="col-xs-12 banner">{{t "club"}}</SortableTableHeader>
  {{/if}}

    <SortableTableHeader @column="landscape" @sortColumn={{sortColumn}} @sortOrder={{sortOrder}} @class="col-xs-12 banner">{{t "landscape"}}</SortableTableHeader>

  {{#if showAirport}}
    <SortableTableHeader @column="airport" @sortColumn={{sortColumn}} @sortOrder={{sortOrder}} @class="airport">{{t "airport"}}</SortableTableHeader>
  {{/if}}

    <th class="column-buttons"></th>
  <th class="num_comments hidden-xs"></th>
</tr>
</thead>
<tbody>
{{#each groupflights as |groupflight|}}
  <GroupflightListRow
    @groupflight={{groupflight}}
    @showDate={{showDate}}
    @showAirport={{showAirport}}
    @showClub={{showClub}} />
{{else}}
  <tr>
    <td colspan="10">{{t "no-flights-found"}}</td>
  </tr>
{{/each}}
</tbody>